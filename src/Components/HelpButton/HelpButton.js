import React, { Component } from 'react'

import './HelpButton.css'

class HelpButton extends Component {

  render() {

    return (
      <button className="task-help-button">
        Использовать подсказку (-1 балл)
      </button>
    )
  }
}

export default HelpButton
